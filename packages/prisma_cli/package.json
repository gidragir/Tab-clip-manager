{
  "name": "@repo/prisma_cli",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "scripts": {
    "migrate": "cargo prisma migrate dev --name init",
    "main": "pwsh -NoExit -Command \"cd (git rev-parse --show-toplevel)\""
  }
}
